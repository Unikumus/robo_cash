import 'dart:ui';

import 'package:flutter/material.dart';

/// main colors
const surface = Color(0xFF444444);
const primary = Color(0xFFFFD21D);
const whiteColor = Colors.white;
const bidColor = Colors.green;
const askColor = Colors.red;
const chartLineV = Color(0xFF8D8D8D);
const chartLineH = Color(0xFF5E5E5E);
const btnColor = Color(0xFFFFD21D);
